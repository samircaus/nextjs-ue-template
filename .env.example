# AEM endpoints for WKND content (GraphQL and assets).
# Copy to .env.local and set for your environment.

# Publish instance: used to fetch GraphQL content and image/asset URLs. If unset, pages show fallback text.
# Example: https://publish-pxxxx-exxxx.adobeaemcloud.com
AEM_PUBLISH_URL=

# Preview tier: used when AEM_USE_PREVIEW_URL=true (e.g. Cloudflare --env preview deployment).
# Example: https://preview-pxxxx-exxxx.adobeaemcloud.com
# AEM_PREVIEW_URL=

# Author instance: only used when server runs in preview mode (to show draft content).
# Set AEM_PREVIEW_MODE=true and start the server to use Author for GraphQL.
# Example: https://author-pxxxx-exxx.adobeaemcloud.com
AEM_AUTHOR_URL=

# Set to "true" or "1" to use AEM Author for GraphQL (preview/draft content). Omit or "false" for production (Publish).
# AEM_PREVIEW_MODE=true

# Set to "true" or "1" to use AEM Preview tier for GraphQL and assets (e.g. Cloudflare deploy with --env preview).
# AEM_USE_PREVIEW_URL=true

# Optional: override image base URL for assets (defaults to AEM_PUBLISH_URL).
# IMAGE_BASE_URL=

# Optional: Universal Editor connection URL (default: https://localhost:8443). Use your AEM Author URL when opening the app in UE.
# AEM_UE_CONNECTION=https://localhost:8443

# Set to "true" or "1" to log each GraphQL query name and URL when making AEM requests (server-side).
# DEBUG=1
# AEM_DEBUG=1
