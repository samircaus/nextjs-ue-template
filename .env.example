# AEM endpoints for WKND content (GraphQL and assets).
# Copy to .env.local and set for your environment.

# Publish instance: used to fetch GraphQL content in production and for all image/asset URLs.
# Example: https://publish-pxxxx-exxxx.adobeaemcloud.com
AEM_PUBLISH_URL=
# NEXT_PUBLIC_AEM_PUBLISH_URL=

# Author instance: only used when server runs in preview mode (to show draft content).
# Set AEM_PREVIEW_MODE=true and start the server to use Author for GraphQL.
# Example: https://author-pxxxx-exxx.adobeaemcloud.com
AEM_AUTHOR_URL=
# NEXT_PUBLIC_AEM_AUTHOR_URL=

# Set to "true" or "1" to use AEM Author for GraphQL (preview/draft content). Omit or "false" for production (Publish).
# AEM_PREVIEW_MODE=true
# NEXT_PUBLIC_AEM_PREVIEW_MODE=

# Optional: override image base URL for assets (defaults to AEM_PUBLISH_URL).
# NEXT_PUBLIC_IMAGE_BASE_URL=

# Set to "true" or "1" to log each GraphQL query name and URL when making AEM requests.
# DEBUG=1
# AEM_DEBUG=1
# NEXT_PUBLIC_AEM_DEBUG=
